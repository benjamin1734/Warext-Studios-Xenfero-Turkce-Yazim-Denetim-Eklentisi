'use strict';
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const file = path.join(__dirname,'../upload/js/warext/turkish-spellcheck/document-v300.js');
const source = fs.readFileSync(file,'utf8');
assert.ok(source.includes("const VERSION = '3.1.3';"));
assert.ok(source.includes('CACHE_LIMIT = 12'));
assert.ok(source.includes('DEEP_SETTLE_MS = 2550'));
assert.ok(source.includes('LIVE_DELAY_MS = 760'));
assert.ok(source.includes('navigator.scheduling?.isInputPending'));
assert.ok(source.includes("mode = deep ? 'settled-hierarchical' : 'live-window'"));
assert.ok(source.includes("deep && st.mode === 'live-window'"));
assert.ok(source.includes('settled:deep'));
assert.ok(source.includes('forceDeep:deep'));
assert.ok(source.includes('scheduleDeep(st,DEEP_SETTLE_MS)'));
assert.ok(source.includes('scheduleDeep(st,140)'));
assert.ok(source.includes('rangeIndex(el)'));
assert.ok(source.includes('getPerformance:el'));
assert.ok(source.includes('clearCache:() => cache.clear()'));
assert.ok(source.includes('forceDeep:el'));
assert.ok(!/https?:\/\//u.test(source));
assert.ok(!/fetch\s*\(|XMLHttpRequest|WebSocket|EventSource|sendBeacon/u.test(source));
console.log('V3.1.3 belge analizi canlı pencere, gecikmeli derin tarama, önbellek ve tek geçişli highlight sözleşmesi başarılı.');
